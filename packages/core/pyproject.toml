[project]
name = "skillforge"
version = "0.1.0"
description = "Skill evolution for AI agents"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
